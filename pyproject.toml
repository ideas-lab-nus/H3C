[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "h3c"
version = "0.1.0"
description = "Hierarchical Causal-Constrained Control (H3C) for cooling-only building control"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "H3C contributors" }]
dependencies = ["pythermalcomfort>=3.6,<4"]

[project.scripts]
h3c = "h3c.cli:main"

[project.optional-dependencies]
dev = [
  "mypy>=1.11",
  "pytest>=8.3",
  "ruff>=0.6",
]
offline = [
  "agent-framework-core==1.15.0",
  "agent-framework-openai==1.14.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/h3c"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["h3c"]
