# H3C context protocol finalization preregistration

## Question and source boundary

This offline-only revision starts from exact source
`8d3eeccd929873f0f2db95367dc282f1b2764519`. It asks whether the three Agent
inputs can close their evidence and contract gaps while retaining the same
control method, output wires, model route, and reversible canonical context.

The revision is limited to:

1. adding already-observed four-action-time external conditions and comfort
   headroom to completed-interval evidence;
2. making the resolved per-zone reserved cap a shared renderer/validator owner;
3. clarifying reserved versus shared-pool settlement and the site-power causal
   citation requirement;
4. replacing ambiguous compact-view labels and implicit rule scope with
   self-describing representations;
5. deriving the Executor operation guidance from the existing parser and
   validator contract; and
6. expanding direct offline contract tests and generated documentation.

## Frozen method boundary

This revision does not change the executable program, patch wire, allocation
wire, Reflector wire, causal admission, Budget calculation or settlement,
Safety, action mapping, experience CRUD, memory references, thinking route,
provider request contract, controller, reward, or case configuration. It adds
no forecast or oracle information to completed history. `hourly_lessons`
remains the public Reflector field. The provider request remains
`thinking.enabled=true` with `reasoning_effort=low` for routed-low calls and
omits `temperature` and `top_p` under that frozen contract.

## Acceptance gates

- Compact views decode exactly to normalized canonical inputs.
- Every new completed-context value is derived from the four already-visible
  action-time observations and is replayed by the verifier.
- The Agent view contains no previous natural-language allocation rationale,
  `common_when`, anonymous adjacent heterogeneous tables, or stale eligible-slot
  label.
- A supplied synthetic reserved cap other than 5 is enforced identically by
  the rendered contract and deterministic validator.
- Allocation causal citations cover missing, duplicate, non-power and valid
  visible power-meter edges.
- All six patch operations cover causal-on/off shape, identifiers, indices and
  value forms through the production parser/validator path.
- Existing historical output JSON parses and replays without migration.
- Full pytest, Ruff, format check, strict mypy, generated-document freshness and
  staged-file secret scan pass.

Character accounting reports both absolute input size and size normalized to
the pre-existing field set. Any absolute increase must be attributable to the
new evidence projection; all other prompt/rendering overhead must not increase.

The only allowed terminal classification is
`DATA-LOSSLESS / CONTRACT-ALIGNED / TIME-EXPLICIT / SEMANTICALLY-EXPLICIT /
EVIDENCE-CLOSED / BEHAVIOR-UNVERIFIED`.

No DeepSeek or BOPTEST call, physical arm, push, paper edit, LaTeX edit, or
Figure 3 edit is authorized by this preregistration.
